spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project?serverTimezone=Asia/Seoul
    username: mydb
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  data:
    mongodb:
      uri: mongodb://localhost:27017/mapinfo
